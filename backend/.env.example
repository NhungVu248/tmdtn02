# XAMPP/local MySQL mặc định: user root, không mật khẩu, port 3306
DATABASE_URL="mysql://root:@localhost:3306/tmdt"

# Đổi thành một chuỗi ngẫu nhiên, dài, giữ bí mật
JWT_SECRET="change_this_to_a_random_secret"
JWT_EXPIRES_IN="7d"

PORT=4000
CLIENT_URL="http://localhost:5173"
